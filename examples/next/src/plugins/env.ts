// import { version as v } from '../../package.json';

export const version = '0.202104.5';

export const isDevelopment = process.env.NODE_ENV === 'development';
export const isStaging = false;

export const notifyAutoClose = 2000;
